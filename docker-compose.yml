version: '3'

services:

  mywww:
    image: bunkerity/bunkerized-nginx
    restart: always
    ports:
      - 80:8080
      - 443:8443
    volumes:
      - ./web-files:/home/docker/fogliano.works:ro
      - ./letsencrypt:/etc/letsencrypt
    environment:
      - SERVER_NAME=www.fogliano.works enzo.fogliano.works
      - MULTISITE=yes
      - AUTO_LETS_ENCRYPT=yes
      - REDIRECT_HTTP_TO_HTTPS=yes
      - DISABLE_DEFAULT_SERVER=yes
      - USE_CLIENT_CACHE=yes
      - USE_GZIP=yes
      - USE_BROTLI=yes
      - REMOTE_PHP=myphp
      - REMOTE_PHP_PATH=/home/docker/fogliano.works/app
      - enzo.fogliano.works_REMOTE_PHP=myenzo
      - enzo.fogliano.works_REMOTE_PHP_PATH=/home/docker/fogliano.works/enzo
    networks:
      - net1
      - net2
      - net3

  myphp:
    image: php:fpm
    restart: always
    volumes:
      - ./web-files:/home/docker/fogliano.works/app

  myenzo:
    image: php:fpm
    restart: always
    volumes:
      - ./web-files/enzo.fogliano.works:/home/docker/fogliano.works/enzo
    networks:
      - net1
  
networks:
  net1:
  net2:
  net3: